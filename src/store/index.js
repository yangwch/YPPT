import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state = {
  cards: []
}

const mutations = {}

const actions = {}

export default new Vuex.Store({
  state,
  mutations,
  actions
})